<?php

return [

    /*
    |--------------------------------------------------------------------------
    | Настройки секций Tailor
    |--------------------------------------------------------------------------
    |
    | Ключ — handle секции (Catalog\Category, Catalog\Product и т.д.)
    |
    | slug_fix (bool, default: true)
    |   Генерировать человекочитаемый slug из title, если slug пуст или
    |   имеет технический вид slug-XXXX. Работает также при ручном
    |   редактировании (безопасно).
    |
    | structure (array|null)
    |   Настройки постобработки для секций типа structure.
    |   Используется командой `php artisan tailor:fix-structure`.
    |
    |   category_field (string)
    |     Поле/атрибут на записи, содержащее значение категории из CSV.
    |     Например: 'category', 'section', 'type'.
    |
    |   parent_resolve (string: 'slug'|'title')
    |     По какому полю искать родительскую запись.
    |
    |   rules (array)
    |     Карта: значение_категории => настройки.
    |     parent: значение поля parent_resolve для поиска родителя.
    |            Если null или не задано — запись помещается в корень.
    |     set: доп. атрибуты, которые нужно выставить этой записи.
    |
    */

    'sections' => [

        // Пример конфигурации для Catalog\Category:
        //
        // 'Catalog\Category' => [
        //
        //     // slug_fix включён по умолчанию глобально через boot().
        //     // Здесь можно явно отключить для секции (не рекомендуется).
        //     // 'slug_fix' => true,
        //
        //     'structure' => [
        //         'category_field' => 'category',   // поле CSV/модели
        //         'parent_resolve' => 'title',       // 'slug' или 'title'
        //         'rules' => [
        //             // Категории верхнего уровня — без родителя
        //             'Ювелирные украшения' => [
        //                 'parent' => null,
        //             ],
        //             'Картины' => [
        //                 'parent' => null,
        //             ],
        //             // Подкатегории — с родителем
        //             'Кольца' => [
        //                 'parent' => 'Ювелирные украшения',
        //             ],
        //             'Серьги' => [
        //                 'parent' => 'Ювелирные украшения',
        //                 // Дополнительные атрибуты (необязательно)
        //                 'set' => [
        //                     'is_enabled' => true,
        //                 ],
        //             ],
        //         ],
        //     ],
        // ],

    ],

];
